# 网页端二维 RTS 游戏 · 需求文档

> 文档版本：v1.0（第一阶段：出兵系统）
> 项目代号：DeepSeek Game RTS
> 更新时间：2025

---

## 1. 项目概述

### 1.1 背景

本项目是一个运行在浏览器中的**二维实时策略（Real-Time Strategy, RTS）游戏**。玩家扮演指挥官，在战场地图上生产古代冷兵器兵种、框选部队、下达移动与攻击指令，与电脑对手（AI）进行实时对抗。

### 1.2 目标

- 实现一个**可直接在浏览器游玩**、具备完整 RTS 核心循环的单人对战 AI 游戏。
- 第一阶段聚焦「**出兵系统**」，暂不引入资源采集与建筑系统。
- 借助 DeepSeek API 为敌方 AI 提供「指挥官级」的策略决策能力，提升对战智能与可玩性；API 不可用时自动降级为规则 AI，保证游戏始终可玩。

### 1.3 范围

| 维度 | 第一阶段（本次） | 后续阶段 |
| --- | --- | --- |
| 资源系统 | ❌ 无采集，仅被动军费增长 | 可加金矿/木材采集 |
| 建筑系统 | ❌ 仅出生点/兵营占位 | ✅ 建筑建造、科技升级 |
| 出兵系统 | ✅ 四兵种生产 | 扩充兵种/英雄单位 |
| 战斗/移动 | ✅ 框选、移动、攻击、索敌 | 阵型、技能、地形 |
| 敌方 AI | ✅ 规则 AI + DeepSeek 增强 | 多级难度、多 AI |

### 1.4 术语表

| 术语 | 说明 |
| --- | --- |
| 人口 / 部队上限 | 玩家与 AI 各自可同时拥有的士兵总数，本作上限 100。 |
| 军费（Gold） | 生产兵种的货币，无采集系统时由时间被动增长。 |
| 兵营（Barracks） | 生产士兵的建筑/设施，第一阶段为固定存在的出生点。 |
| 框选（Box Select） | 鼠标拖拽出矩形，选中矩形内己方多个单位。 |
| 攻击移动（A 键 / Attack Move） | 部队向目标点移动，途中自动攻击进入射程的敌人。 |
| 训练时间 | 一个兵种从下达生产指令到单位出场的耗时。 |
| 索敌 | 单位自动寻找并攻击射程内敌人。 |

---

## 2. 运行环境与技术约束

### 2.1 可用环境

- **运行环境**：仅提供 **Node.js** 与 **DeepSeek API**。
- 无云服务器、无数据库、无第三方前端框架依赖的硬性要求。

### 2.2 技术选型

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 后端 | Node.js（内置 `http` 模块，零依赖优先） | 静态资源服务器 + DeepSeek API 代理 |
| 前端 | HTML5 Canvas 2D + 原生 JavaScript | 无构建步骤，直接加载运行 |
| 样式 | 原生 CSS | HUD 与界面 |
| 通信 | HTTP / JSON | 前端 ↔ 后端仅用于 AI 决策 |

> 选择 Canvas 2D 的原因：无需构建工具、跨浏览器稳定、2D 绘制性能足以支撑 100+ 单位。

### 2.3 目录结构（规划）

```text
deepseek_game_rts/
├── 需求文档.md
├── README.md
├── package.json
├── server.js                 # 静态服务器 + /api/ai 代理
├── .env.example              # DEEPSEEK_API_KEY 示例
├── public/
│   ├── index.html
│   ├── css/
│   │   └── style.css
│   └── js/
│       ├── main.js           # 入口 + 游戏主循环
│       ├── config.js         # 兵种/平衡/常量配置
│       ├── input.js          # 鼠标/键盘/框选交互
│       ├── camera.js         # 相机与视野
│       ├── world.js          # 地图与静态实体
│       ├── unit.js           # 单位实体与状态机
│       ├── pathfinding.js    # 寻路（网格 A*）
│       ├── combat.js         # 战斗/索敌/伤害结算
│       ├── production.js     # 生产队列
│       ├── ai.js             # 规则 AI + DeepSeek 指挥官
│       ├── render.js         # 渲染
│       └── ui.js             # HUD/面板/提示
└── assets/                   # 占位图片/音效（可选）
```

---

## 3. 核心玩法需求

### 3.1 对局时长

- **R-001**：默认难度下，一场完整对局（开始 → 一方取胜）**平均时长 ≥ 10 分钟**，最长不超过 20 分钟。
- 通过「被动军费增长速率 + 兵种训练时间 + 敌方 AI 的波次节奏 + 基地耐久」综合控制节奏，使玩家无法在几分钟内速胜，也无法因无资源而停滞。

### 3.2 胜负条件

- **R-002**：双方各有一个基地（指挥所）。**摧毁敌方基地即获胜**；己方基地被摧毁则失败。
- **R-003**：基地拥有高耐久（HP），且随对局时间获得轻微防御修正，防止秒杀，保障 10 分钟底线。
- 平局：若对局超过 20 分钟仍无胜负，按剩余兵力 + 基地耐久综合判胜。

### 3.3 兵种体系（四类古代冷兵器兵种）

- **R-004**：提供 4 种可生产兵种：**长矛兵、刀盾兵、弓箭手、骑兵**。
- **R-005**：兵种之间存在**克制关系**（见第 4 章），鼓励玩家搭配兵种而非单一兵种海。
- **R-006**：每种兵种具备独立属性（生命、攻击、射程、攻速、移速、成本、训练时间）与差异化外观（形状/颜色）。

### 3.4 出兵（生产）系统

- **R-007**：玩家可通过**兵营面板**或**快捷键（Q/W/E/R）**下达生产指令。
- **R-008**：生产采用**队列**机制，同一时刻可排队多个单位，逐个训练、逐个出场。
- **R-009**：生产需要消耗**军费**，军费不足时按钮置灰、无法下单。
- **R-010**：单位训练完成后，从兵营位置出生并加入玩家部队。

### 3.5 部队上限

- **R-011**：**士兵总数上限 = 100**（玩家与 AI 各自独立计算）。
- **R-012**：达到上限后禁止继续生产（按钮置灰并提示「部队已满」）。
- **R-013**：上限仅约束己方可同时存在的士兵，不约束敌方。

### 3.6 操作与玩法（类常规 RTS）

- **R-014**：**左键拖拽框选**：拉出矩形，选中矩形内全部己方单位（可多选）。
- **R-015**：**左键点选**：点击单个单位进行单选；点击空白处取消选择。
- **R-016**：**右键移动**：被选中部队向目标点移动（保持松散编队）。
- **R-017**：**右键攻击 / 攻击移动（A 键）**：对敌方单位下达攻击指令；A+左键为攻击移动，途中自动索敌攻击。
- **R-018**：**自动索敌**：静止或移动中的单位，会在射程内自动攻击最近敌方单位。
- **R-019**：单位移动需绕开地图边界与不可通行区域，采用寻路算法（网格 A*）。

### 3.7 无资源采集（第一阶段限定）

- **R-020**：第一阶段**没有资源采集系统**（无矿场、无农民、无采集单位）。
- **R-021**：军费通过**时间被动增长**获得（见 5.1），作为生产的唯一经济来源。

---

## 4. 兵种详细设计

> 以下数值为**初始平衡基线**，集中定义在 `config.js`，便于后续调参。所有数值可在开发中调整。

### 4.1 属性表

| 兵种 | 生命 | 攻击 | 射程 | 攻击间隔 | 移速 | 成本(军费) | 训练时间 | 人口 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 长矛兵 Spearman | 110 | 16 | 1.4 | 1.2s | 2.2 | 60 | 6s | 1 |
| 刀盾兵 Swordsman | 150 | 13 | 1.0 | 1.0s | 2.0 | 70 | 8s | 1 |
| 弓箭手 Archer | 70 | 13 | 5.5 | 1.4s | 2.0 | 80 | 9s | 1 |
| 骑兵 Cavalry | 130 | 20 | 1.0 | 1.5s | 4.2 | 120 | 12s | 1 |

> 射程：1.0 表示贴身近战；弓箭手为远程。攻击间隔：两次攻击间秒数。人口：每单位占用人口（本作统一为 1）。

### 4.2 克制关系（伤害倍率表）

行 = 攻击方，列 = 受击方。数值为伤害乘数（>1 克制，<1 被克制）。

| 攻击方 ＼ 受击方 | 长矛兵 | 刀盾兵 | 弓箭手 | 骑兵 |
| --- | ---: | ---: | ---: | ---: |
| 长矛兵 | 1.0 | 1.0 | 1.0 | **1.6** |
| 刀盾兵 | 1.2 | 1.0 | **1.3** | 0.8 |
| 弓箭手 | 1.0 | 0.7（盾牌减伤） | 1.0 | 1.0 |
| 骑兵 | 0.7 | 1.1 | **1.5** | 1.0 |

### 4.3 兵种定位

- **长矛兵**：廉价的抗线/反骑兵单位，克制骑兵，性价比高，前排主力之一。
- **刀盾兵**：高生命的肉盾，克制长矛与弓箭（持盾减伤），但被骑兵冲击克制。
- **弓箭手**：远程输出，克制近战集群（风筝），但脆皮且被骑兵快速切入。
- **骑兵**：高速高攻的突击单位，克制弓箭与刀盾，但被长矛兵的反骑兵加成克制。

---

## 5. 游戏系统设计

### 5.1 经济系统（被动军费）

- **E-001**：初始军费：玩家与 AI 各 **300**。
- **E-002**：基础被动增长：**+5 军费/秒**。
- **E-003**：随时间成长：每过 60 秒，增长率 **+0.5/秒**，封顶 **+10/秒**（加速中后期节奏，避免僵持）。
- **E-004**：军费上限 **2000**（防溢出囤积）。

### 5.2 生产系统

- **P-001**：生产队列独立于战斗，按「下单顺序 → 训练时间到 → 出生」执行。
- **P-002**：出生点 = 兵营附近随机偏移位置（避免单位重叠）。
- **P-003**：支持取消排队中（尚未开始训练）的单位，返还全额军费；训练中的单位取消返还 50%。

### 5.3 选择系统

- **S-001**：框选矩形（拖拽）与单选（点击）两种方式。
- **S-002**：被选中单位高亮（血条 + 选中圈）。
- **S-003**：支持 `Shift` 追加选择、`Esc`/右键空白取消选择。

### 5.4 移动系统

- **M-001**：移动采用**网格 A\* 寻路** + 局部避障（单位间小范围排斥，防止重叠堆叠）。
- **M-002**：多单位移动时以「目标点 + 相对偏移」形成松散编队，避免挤成一团。
- **M-003**：骑兵移速明显快于步兵，用于快速支援与突袭。

### 5.5 战斗系统

- **C-001**：单位自动索敌：优先攻击射程内**最近**敌方单位。
- **C-002**：近战单位需贴近（射程内）方可攻击；远程单位在射程内即攻击。
- **C-003**：伤害结算：`实际伤害 = 攻击力 × 克制倍率`，向下取整，最低 1 点。
- **C-004**：攻击有前摇/间隔，间隔内不重复攻击。
- **C-005**：单位死亡后移除，并释放其占用的 1 人口。
- **C-006**：攻击移动（A+左键）下，部队朝目标推进途中自动攻击进入射程的敌人。

### 5.6 相机与地图

- **V-001**：地图尺寸约 **64×64 格**（每格约 48px），开局玩家与 AI 分居左右两侧对称分布。
- **V-002**：支持**鼠标滚轮缩放**、**方向键/边缘移动** 平移视角（可选空格回到基地）。
- **V-003**：小地图（HUD 角标）显示敌我单位概览与视野范围。

### 5.7 敌方 AI 系统

- **A-001**：AI 与玩家规则对等：同样受军费、训练时间、100 人口上限约束（保证公平）。
- **A-002**：AI 行为分两层：
  1. **规则层（保底）**：按内置策略周期性地生产兵种、集结、进攻、回防，逻辑完全确定性。
  2. **DeepSeek 指挥官层（增强）**：定期（每 20–30 秒）向 DeepSeek 请求一次「高层策略决策」，覆盖/微调规则层行为（见第 8 章）。
- **A-003**：DeepSeek 调用失败、超时或返回非法结果时，**自动降级**为规则层，游戏不中断、不报错。

---

## 6. UI / UX 设计

### 6.1 HUD（抬头显示）

- 顶部：军费（当前值/增长率）、当前人口 / 100、对局时间。
- 底部：生产面板（四兵种按钮，显示图标、名称、成本、快捷键）。
- 右侧：小地图。
- 中央：选中单位信息（数量、类型构成、可下达指令提示）。

### 6.2 快捷键

| 按键 | 功能 |
| --- | --- |
| 左键拖拽 | 框选 |
| 左键点击 | 单选 / 点选按钮 |
| 右键 | 移动 / 攻击指定目标 |
| A + 左键 | 攻击移动 |
| Q / W / E / R | 生产 长矛兵 / 刀盾兵 / 弓箭手 / 骑兵 |
| Shift + 框选/点击 | 追加选择 |
| Esc | 取消选择 / 关闭面板 |
| 空格 | 视角回到基地 |
| 滚轮 / 方向键 | 缩放 / 平移视角 |

### 6.3 视觉反馈

- 选中高亮、移动目标点光标、攻击目标标识、单位血条、伤害飘字（可选）、生产进度条。

### 6.4 提示

- 军费不足 / 人口已满 / 训练完成 等给出明确文字提示（toast）。
- AI 决策来源（规则 / DeepSeek）在调试面板中可见（便于验证第 8 章集成）。

---

## 7. 数据模型（概要）

```js
// 单位
Unit {
  id, owner: 'player' | 'enemy', type: 'spear'|'sword'|'archer'|'cavalry',
  x, y, hp, maxHp, state: 'idle'|'move'|'attack'|'dead',
  targetId?, path: [..], attackCooldown, attackWindup
}

// 生产队列项
ProductionOrder { type, cost, totalTime, elapsed, status: 'queued'|'training' }

// 玩家/阵营状态
Faction {
  gold, goldRate, population, populationCap: 100,
  baseHp, baseMaxHp, productionQueue: [..], units: Map<id, Unit>
}

// 对局状态
GameState {
  time, phase: 'running'|'victory'|'defeat'|'draw',
  player: Faction, enemy: Faction, world: {width, height, grid}
}
```

---

## 8. DeepSeek API 集成方案

### 8.1 用途

DeepSeek API 作为**敌方 AI 的「指挥官」**：接收战场态势摘要，输出高层策略（生产倾向、进攻时机、主攻方向），规则层负责把策略落地为具体单位指令。这样既避免高频调用（成本/延迟可控），又能让 AI 表现更智能。

### 8.2 接口约定

- 端点：`POST https://api.deepseek.com/chat/completions`
- 模型：`deepseek-v4-pro`
- 参数：`thinking: {type:'enabled'}`、`reasoning_effort:'high'`、`stream:false`
- 鉴权：`Authorization: Bearer <DEEPSEEK_API_KEY>`

### 8.3 请求示例（后端代理发出）

```json
{
  "model": "deepseek-v4-pro",
  "messages": [
    {
      "role": "system",
      "content": "你是 RTS 游戏的敌方指挥官。你只能回复一个合法 JSON，不要输出任何其他文字。JSON 字段：armyFocus(兵种倾向，值为 spear/sword/archer/cavalry 之一)，aggression(0-100 进攻倾向)，attackNow(布尔)，comment(不超过30字说明)。"
    },
    {
      "role": "user",
      "content": "{\"time\":420,\"myGold\":850,\"myPop\":62,\"enemyPop\":58,\"myArmy\":{\"spear\":20,\"sword\":12,\"archer\":8,\"cavalry\":22},\"enemyArmy\":{\"spear\":15,\"sword\":10,\"archer\":18,\"cavalry\":15},\"baseHp\":2000,\"enemyBaseHp\":1800}"
    }
  ],
  "thinking": {"type": "enabled"},
  "reasoning_effort": "high",
  "stream": false
}
```

### 8.4 前端 ↔ 后端流程

```text
浏览器(游戏) --POST /api/ai/command (状态摘要)--> Node.js 后端
Node.js 后端 --POST DeepSeek chat/completions--> DeepSeek API
Node.js 后端 <--JSON 决策-- DeepSeek API
浏览器(游戏) <--JSON 决策-- Node.js 后端
```

### 8.5 安全与配置

- **S-101**：API Key **仅保存在后端**（环境变量 `DEEPSEEK_API_KEY`），**绝不出现在前端代码或静态资源中**。
- **S-102**：前端通过后端 `/api/ai/command` 代理调用，后端负责鉴权、超时、错误处理。
- **S-103**：提供 `.env.example` 与读取逻辑，Key 缺省时 AI 自动进入纯规则模式。

### 8.6 频率、超时与降级

| 项 | 策略 |
| --- | --- |
| 调用频率 | 每 **20–30 秒**最多 1 次（对战约 10 分钟 ≈ 20–30 次） |
| 超时 | 单次请求 **5 秒**超时 |
| 降级 | 失败/超时/非法 JSON → 使用规则 AI 决策，本次不重试 |
| 成本控制 | 仅在「真实对局」中调用，主菜单/暂停不调用 |

---

## 9. 非功能性需求

### 9.1 性能

- **N-001**：在 100 vs 100 单位同屏战斗时，目标 **≥ 30 FPS**（主流笔记本浏览器）。
- **N-002**：寻路与索敌采用空间分桶/网格，避免 O(n²) 全量比较。
- **N-003**：渲染仅绘制视野内对象，超视野单位裁剪。

### 9.2 兼容与可玩性

- **N-004**：支持最新版 Chrome / Edge / Firefox。
- **N-005**：无网络（DeepSeek 不可达）时游戏仍可完整游玩（规则 AI）。

### 9.3 可维护性

- **N-006**：所有平衡数值集中在 `config.js`，调参无需改动逻辑代码。
- **N-007**：模块化拆分（见 2.3），职责单一。

---

## 10. 验收标准（第一阶段）

| 编号 | 验收项 | 通过条件 |
| --- | --- | --- |
| AC-01 | 可运行 | `node server.js` 后浏览器打开首页即可进入并开始对局 |
| AC-02 | 生产 | 可生产四类兵种，消耗军费、有训练时间、按队列出场 |
| AC-03 | 上限 | 部队达到 100 后无法继续生产 |
| AC-04 | 选择/移动 | 可框选多单位、右键移动，单位按寻路到达目标 |
| AC-05 | 战斗 | 单位可自动索敌攻击，攻击移动有效，克制倍率生效 |
| AC-06 | 对局时长 | 默认对局平均 ≥ 10 分钟，可正常分出胜负 |
| AC-07 | AI 对抗 | 敌方 AI 能生产、集结、进攻、回防，构成有效对抗 |
| AC-08 | AI 增强 | DeepSeek 可用时 AI 采用其策略；不可用时自动降级且不报错 |
| AC-09 | 无采集 | 全流程不存在任何采集单位/机制，仅被动军费 |

---

## 11. 里程碑

- **M1（本次）**：需求文档 + 项目骨架（Node 服务器、静态前端、Canvas 主循环、地图与相机）。
- **M2**：单位实体 + 生产系统 + 经济（被动军费）+ 100 人口上限。
- **M3**：选择/框选 + 移动 + 寻路 + 索敌 + 战斗与克制结算。
- **M4**：敌方规则 AI + DeepSeek 指挥官接入 + 降级策略。
- **M5**：HUD/UI/小地图/提示 + 胜负判定 + 性能优化与验收。
- **M6（后续阶段）**：建筑系统（兵营、防御塔、基地升级）、资源采集、更多兵种与科技。

---

## 12. 风险与假设

| 风险/假设 | 说明 | 应对 |
| --- | --- | --- |
| 平衡性未知 | 数值为初始基线，未经过实战验证 | 数值集中配置，可快速调整 |
| DeepSeek 延迟 | 单次 5s 超时可能滞后决策 | 异步调用 + 规则层兜底，不阻塞游戏循环 |
| API 成本 | 调用过多产生费用 | 限频 20–30s/次，仅真实对局调用 |
| 性能 | 200 单位同屏 | 空间分桶、视野裁剪、简化渲染 |
| 图像素材 | 首版用几何占位图形 | 后续可替换为美术素材 |

---

## 13. 待确认事项（开放问题）

1. 是否需要**音效/背景音乐**（首版可纯视觉）。
2. 敌方 AI 难度是否要分档（简单/普通/困难）。
3. 兵种数值基线是否认可，或希望先以某套数值为准。
4. 是否需要**多人联机**（当前方案为单机 vs AI，联机需后续扩展）。
