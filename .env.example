# DeepSeek Game RTS - 环境变量示例
# 复制本文件为 .env 并填入真实 Key 后重启服务。
# Key 缺省时游戏自动进入纯规则 AI 模式（降级），不影响游玩。

DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 可选：覆盖默认端口（默认 3000）
PORT=3000

# 可选：覆盖模型（默认 deepseek-v4-flash，已关闭思考模式）
DEEPSEEK_MODEL=deepseek-v4-flash
